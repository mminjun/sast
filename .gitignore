# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python

# 가상환경
venv/
.venv/
env/

# Django
*.log
/logs/
local_settings.py
db.sqlite3
db.sqlite3-journal

# 분석 작업 영역 (업로드된 소스·실행별 격리 디렉토리)
/media/

# 환경변수 (시크릿) — 템플릿만 커밋한다
.env
.env.*
!.env.example

# 프론트엔드 (React/Vite)
frontend/node_modules/
frontend/dist/

# 도그푸딩·시연 자료 — 소스(demo-app/)만 커밋하고 zip은 빌드 산출물이라 제외
# (zip은 Compress-Archive로 소스에서 재생성)
dogfood/*.zip