# 외부 구성요소 버전 고정 (SEC-010)
# 취약점 공지·릴리즈 노트를 추적할 수 있도록 직접 의존성만 정확한 버전으로 고정한다.
# (asgiref / sqlparse / tzdata / pyjwt는 아래 패키지들이 끌어오는 전이 의존성이라 제외)
# 설치: pip install -r requirements.txt

Django==6.1
djangorestframework==3.18.0
psycopg[binary]==3.3.4
python-dotenv==1.2.3

# 인증 (SFR-002) / 비밀번호 해시 (SEC-001)
djangorestframework-simplejwt==5.5.1
bcrypt==5.0.0

# 분석 엔진 (SFR-008~009, SEC-010)
# 라이선스: LGPL-2.1 (github.com/semgrep/semgrep/blob/develop/LICENSE, 확인일 2026-08-27).
# subprocess로 외부 CLI를 호출만 하고 소스를 정적/동적 링크하지 않으므로 카피레프트
# 의무가 우리 코드로 전이되지 않는다 (plan.md 6절 "직접 진단 로직 구현 안 함"과 일치).
semgrep==1.175.0

# 진단 기준 카탈로그 (SFR-012, SFR-013)
# seed_catalog 커맨드가 catalog/rules/*.yaml의 metadata.kisa_code를 읽어 룰↔KISA 항목
# 매핑을 갱신하는 데 쓴다. semgrep도 YAML을 다루지만 ruamel.yaml을 쓰고 PyYAML을
# 끌어오지 않으므로(pip show pyyaml의 Required-by 비어 있음) 전이 의존성이 아니라
# 우리 직접 의존성이다. 라이선스: MIT.
PyYAML==6.0.3

# 분석 실행 백그라운드 큐 (SFR-008~009, SFR-015, SEC-009)
# Django 6.1 내장 Tasks 프레임워크(django.tasks)의 DB 백엔드 — 큐를 PostgreSQL 테이블로 두어
# Redis 등 새 인프라 없이 워커(manage.py analysis_worker)가 작업을 가져간다. Celery·RQ·huey를
# 쓰지 않은 근거는 docs/decisions.md 2026-09-06. 라이선스: BSD-3-Clause.
# (typing_extensions / django-stubs-ext는 이 패키지가 끌어오는 전이 의존성)
django-tasks-db==0.13.0

# 컨테이너 실행 (docs/decisions.md 2026-09-07 Docker 한 줄 실행)
# whitenoise — Django가 프론트 빌드(frontend/dist)와 admin 정적 파일을 직접 서빙한다. nginx 컨테이너를
# 두지 않는 근거는 decisions 참조. 전이 의존성 없음. 라이선스: MIT.
whitenoise==6.12.0
# gunicorn — 컨테이너 안 WSGI 서버. runserver는 개발 전용이라 Docker 이미지에서는 쓰지 않는다. 호스트
# 개발 경로(runserver)는 그대로다. 전이 의존성 없음. 라이선스: MIT.
gunicorn==26.2.0
